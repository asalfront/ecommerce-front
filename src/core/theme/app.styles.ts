import { styled } from '@mui/system';

export const Title = styled('div')({
	color: '#41C1BE',
	fontSize: '45px',
	fontWeight: 600,
	padding: 8,
});

export const appStyles = {
	fullHeight: {
		height: '100vh'
	} as const
};
