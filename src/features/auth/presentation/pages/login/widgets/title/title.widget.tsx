import { Grid } from '@mui/material';
import { Title } from '../../login.styles';

const TitleWidget = () => {
	return (
		<>
			<Grid item>
				<Title>Login</Title>
			</Grid>
		</>
	);
};

export default TitleWidget;
